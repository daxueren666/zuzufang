# rent-assist runtime dependencies (clean.py uses stdlib only; collect/render need the packages below)
requests
jinja2
pillow         # fetch_media.py image compression (long edge <=1024, JPEG q82)

# Optional:
# playwright   # Douban fallback rendering (collect_douban degraded mode)
# Video transcription does not use funasr: sherpa-onnx lives in a separate venv <tools>\asr-venv
#              (not listed here; asr.py exits 3 with setup guide if deps are missing)
